
import scala.collection.mutable
/**
  * 4.1	
  Implement a function to check if a tree is balanced. 
  For the purposes of this question, a balanced tree is defined to be a tree such that no two leaf nodes differ in distance
  from the root by more than one.
  */

//abstract class Node[T] {
//	val data : T
//	val parent : Node[T]
//	val children : mutable.ArrayBuffer[Node[T]]
//}
//
//abstract class Tree[T] {
//  val head : Node[T]
//
//  def add(data : T){
//
//  }
//
//}

object TreeImpl {
	def main(args : Array[String]) {
		println("main")
	}
}
